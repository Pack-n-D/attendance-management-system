name: Backend Railway Integration

on:
  workflow_dispatch:

jobs:
  railway-info:
    runs-on: ubuntu-latest
    steps:
      - name: Railway Integration Status
        run: echo "Railway backend is deployed automatically via Railway's native GitHub integration."
